DATABASE_URL=
JWT_SECRET=
DEMO_LOGIN_EMAIL=

# Required before production deployment — gates GraphQL Playground/introspection
# (see app.module.ts). Not currently set anywhere (no hosting platform configured yet).
# NODE_ENV=production